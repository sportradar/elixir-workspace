[
  name: :<%= name %>,
  shortdoc: "Add a short description of your template here",
  doc: """
  Add a detailed description.
  """,
  args: [
    # provide if needed a list of CLI arguments the template expects
    #
    # it should be a keyword list adhering to the cli_opts format
  ]
]
